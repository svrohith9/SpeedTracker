import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.svrohith9.speedtracker',
  appName: 'SpeedTracker',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
